const config = {
  apiUrl: 'http://localhost:5000',
  endpoints: {
    auth: '/auth',
    holdings: '/allHoldings',
    positions: '/allPositions',
    orders: '/newOrder',
  }
};

export default config; 